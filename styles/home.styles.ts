import styled from 'styled-components';

const HomeStyles = styled.div`
	background-color: ${({ theme }) => theme.scaffoldBackgroundColor};
	padding: 1rem;
	height: 100vh;
	width: 100vw;
`;

export default HomeStyles;
